import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' // 우리가 아까 만든 App.tsx

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

---

### 🚀 다시 실행하기
이제 모든 준비가 끝났습니다! 터미널에 아래 명령어를 입력해 보세요.
```
